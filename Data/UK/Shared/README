The "deathsByAge.dat" is not used anymore in sims, but the code still expects a file by that name (we will modified the behaviour at some point in the future).  That is why for the UK the data is just taken from another country (but not used).
